"""
创建函数，计算加速度，初始和结束的速度为v1和v2，初始和结束的时间为t1和t2
然后，用参数(v1, v2, t1, t2)分别为(0, 10, 0, 20)调用函数，得到结果为：
0.5
"""
def v_t(v1,v2,t1,t2):
    if t2 - t1 == 0:
        print('时间间隔不能为0！')
    else:
        a = (v2 - v1) / (t2 - t1)
        print(a)
v_t(0, 10, 0, 20)